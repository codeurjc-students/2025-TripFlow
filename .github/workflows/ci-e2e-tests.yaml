name: CI - E2E Tests

on:
  push:
    branches:
      - main
      - develop
    paths:
      - 'backend/**'
      - 'frontend/**'
      - 'e2e/**'
      - '.github/workflows/ci-e2e-tests.yaml'
  pull_request:
    branches:
      - main
    paths:
      - 'backend/**'
      - 'frontend/**'
      - 'e2e/**'
      - '.github/workflows/ci-e2e-tests.yaml'
  
  workflow_dispatch:

jobs:
  e2e-testing:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          # Pinned to 22: Node 24 has a stream regression (nodejs/node#63487)
          # that hangs Playwright's browser download at 100% (microsoft/playwright#40724).
          node-version: '22'

      - name: Set up Docker Compose
        uses: docker/setup-compose-action@v1
        with:
          version: latest

      - name: Start services with Docker Compose
        working-directory: ./docker
        run: docker compose --env-file .env.example -f docker-compose.test.yaml up -d

      - name: Wait for services to be healthy
        working-directory: ./scripts
        run: |
          chmod +x run-wait-for-services.sh
          ./run-wait-for-services.sh

      - name: Install E2E dependencies
        working-directory: ./e2e
        run: npm ci

      - name: Get Playwright version
        id: playwright-version
        working-directory: ./e2e
        run: echo "version=$(node -e "console.log(require('@playwright/test/package.json').version)")" >> "$GITHUB_OUTPUT"

      - name: Cache Playwright browsers
        id: playwright-cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ steps.playwright-version.outputs.version }}

      - name: Install Playwright Browsers (full, on cache miss)
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        working-directory: ./e2e
        run: npx playwright install --with-deps

      - name: Install Playwright OS deps only (on cache hit)
        if: steps.playwright-cache.outputs.cache-hit == 'true'
        working-directory: ./e2e
        run: npx playwright install-deps
      
      - name: Run Playwright tests
        working-directory: ./e2e
        run: npx playwright test